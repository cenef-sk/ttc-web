import { Injectable } from '@angular/core';

Injectable()
export class Globals{
    world = {}; //delete this
    token = null;
    user = null;
    org = null;
    trialId = null; //delete this
    game = {
      background: null,
      activities: [],
      gameObj: null
    };
}
